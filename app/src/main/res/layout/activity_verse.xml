<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    android:padding="0dp">

    <!-- Subtle Om symbol watermark -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ॐ"
        android:textColor="#10FF8C00"
        android:textSize="280sp"
        android:layout_centerInParent="true"
        android:fontFamily="serif" />

    <!-- Verse Card (centered) -->
    <LinearLayout
        android:id="@+id/cardVerse"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginHorizontal="24dp"
        android:orientation="vertical"
        android:background="@drawable/verse_card_background"
        android:padding="24dp"
        android:elevation="8dp">

        <!-- Verse ID -->
        <TextView
            android:id="@+id/tvVerseId"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Chapter 2, Verse 47  •  2.47"
            android:textColor="#FF8C00"
            android:textSize="12sp"
            android:letterSpacing="0.15"
            android:textAllCaps="false"
            android:fontFamily="sans-serif-medium"
            android:layout_marginBottom="16dp" />

        <!-- Gold divider -->
        <View
            android:layout_width="40dp"
            android:layout_height="2dp"
            android:background="#FF8C00"
            android:layout_marginBottom="16dp" />

        <!-- English verse -->
        <TextView
            android:id="@+id/tvEnglish"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="English verse text here"
            android:textColor="#FFFFFF"
            android:textSize="17sp"
            android:lineSpacingMultiplier="1.55"
            android:fontFamily="serif"
            android:layout_marginBottom="16dp" />

        <!-- Thin separator between English and Bengali -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#22FF8C00"
            android:layout_marginBottom="14dp" />

        <!-- Bengali verse -->
        <TextView
            android:id="@+id/tvBengali"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="বাংলা শ্লোক এখানে"
            android:textColor="#CCCCCC"
            android:textSize="15sp"
            android:lineSpacingMultiplier="1.6"
            android:fontFamily="sans-serif" />

    </LinearLayout>

    <!-- Countdown progress bar at bottom -->
    <ProgressBar
        android:id="@+id/progressTimer"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:layout_alignParentBottom="true"
        android:progressTint="#FF8C00"
        android:progressBackgroundTint="#222222"
        android:max="15000"
        android:progress="15000" />

    <!-- Tap hint -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tap anywhere to dismiss"
        android:textColor="#44FFFFFF"
        android:textSize="11sp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="10dp"
        android:letterSpacing="0.08" />

    <!-- App label top right -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ॐ  Gita Darshan"
        android:textColor="#55FF8C00"
        android:textSize="11sp"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_margin="20dp"
        android:fontFamily="serif" />

</RelativeLayout>
